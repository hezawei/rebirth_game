# This is an example environment file.
# Copy this file to .env and fill in your actual configuration values.

# --- Database Configuration ---
# The application is configured to use a local SQLite database by default.
# The path is relative to the directory where the app runs (rebirth_game/).
# For production, you might want to switch to PostgreSQL, e.g.:
# DATABASE_URL="postgresql://user:password@host:port/dbname"
DATABASE_URL="sqlite:///rebirth_game.db"

# --- API Keys (Optional) ---
# At least one LLM API key is required to run the story engine.
OPENAI_API_KEY=""
DOUBAO_API_KEY=""
GOOGLE_API_KEY=""
ANTHROPIC_API_KEY=""
