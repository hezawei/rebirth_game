# This is an example environment file.
# Copy this file to .env and fill in your actual configuration values.

# --- Database Configuration (PostgreSQL only) ---
# Required: PostgreSQL connection string (SQLAlchemy/psycopg2 format)
# If your password contains special characters (@, :, /, #), percent-encode them.
# Example: my@pass -> my%40pass
DATABASE_URL="postgresql+psycopg2://user:password@host:5432/dbname"

# Enforce vendor to avoid accidental mixing
ENFORCE_DB_VENDOR=postgresql

## (Deprecated) Previous local SQLite setup — do NOT use after migration
# DATABASE_URL="sqlite:///rebirth_game.db"

## Gunicorn worker processes (Docker). For PostgreSQL, a common rule is CPU*2+1.
# GUNICORN_WORKERS=5

## --- API Keys (Optional) ---
# At least one LLM API key is required to run the story engine.
OPENAI_API_KEY=""
DOUBAO_API_KEY=""
GOOGLE_API_KEY=""
ANTHROPIC_API_KEY=""
